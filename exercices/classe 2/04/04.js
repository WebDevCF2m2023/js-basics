function isPositiveOrNegative(number) {
    if (number > 0) {
        console.log('Positive');
    } else if (number < 0) {
        console.log('Negative');
    } else {
        console.log('Zero');
    }
    }

isPositiveOrNegative(5);
isPositiveOrNegative(-5);